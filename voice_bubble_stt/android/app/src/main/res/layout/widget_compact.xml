<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_glass_bg"
    android:orientation="vertical"
    android:padding="12dp"
    android:gravity="center_horizontal">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="VoiceBubble"
        android:textSize="10sp"
        android:textColor="#6B7A90"
        android:letterSpacing="0.06"
        android:textStyle="bold"
        android:textAllCaps="true" />

    <FrameLayout
        android:id="@+id/widget_mic_wrap"
        android:layout_width="84dp"
        android:layout_height="84dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/widget_mic_bg">

        <ImageView
            android:id="@+id/widget_mic_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_mic"
            android:tint="#007AFF" />
    </FrameLayout>

    <TextView
        android:id="@+id/widget_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tocar para dictar"
        android:textSize="12sp"
        android:textColor="#0B1220"
        android:textStyle="bold"
        android:gravity="center" />

    <TextView
        android:id="@+id/widget_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Groq turbo"
        android:textSize="10sp"
        android:textColor="#6B7A90"
        android:gravity="center" />

</LinearLayout>
